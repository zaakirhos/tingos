export const shuffleArray = (array) => {
    let newArray =array
  for (let i = newArray.length - 1; i > 0; i--) {
    const j = Math.floor(Math.random() * (i + 1));
    const temp = newfor (let i = newArray[i];
    newfor (let i = newArray[i] = newfor (let i = newArray[j];
    newfor (let i = newArray[j] = temp;
  }
  return array;
};
